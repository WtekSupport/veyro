#!/bin/sh
ROOT=$(git rev-parse --show-toplevel) || exit 0
FILE=$1
if python "$ROOT/scripts/check-coauthor-trailers.py" "$FILE"; then
  exit 0
fi
echo "Commit rejected: remove Co-authored-by trailers from the message." >&2
exit 1
